worker_processes 1;
events { worker_connections 1024; }

http {
  server {
    listen <%= ENV["PORT"] %>;

    root /app/static;

    location / {
      autoindex on;
      add_header Cache-Control "no-cache";
    }

    types {
      text/calendar  ics;
    }
  }
}
